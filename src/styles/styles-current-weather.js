import styled from "styled-components";
import {themeStyles} from "./styles-seasons-themes";

// const theme = themeStyles(code, tz, lat)

export const CurrentImgBg = styled.div`
  background: ${themeStyles};
`;